# Test Environment Configuration
PORT=5000
NODE_ENV=test
HOST=0.0.0.0

# JWT Configuration (for testing only)
JWT_SECRET=test-secret-key-for-ci-pipeline-only
JWT_EXPIRES_IN=24h

# Database (test database)
DATABASE_PATH=./backend/test_itsm.db

# CORS Configuration
CORS_ORIGIN=*

# Logging
LOG_LEVEL=error

# Cache Configuration (disabled for test stability)
CACHE_ENABLED=false
