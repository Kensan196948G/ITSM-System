# ITSM-Sec Nexus - 環境分離ベストプラクティス

## 🎯 概要

開発環境と本番環境を分離することで、安全で効率的な開発・運用が可能になります。

---

## 📊 環境構成

### 現在の構成

| 項目 | 開発環境 | 本番環境 |
|------|---------|---------|
| **URL** | `https://192.168.0.187:5443` | `https://192.168.0.187:6443` |
| **プロトコル** | HTTPS | HTTPS |
| **ポート** | 5443 | 6443 |
| **NODE_ENV** | development | production |
| **ログレベル** | debug | info |
| **サンプルデータ** | 有効 | 無効 |
| **レート制限** | 緩い (1000req/min) | 厳しい (100req/min) |
| **バッジ色** | 緑色 🔧 | 赤色 🔒 |
| **タイトル** | `[開発]` | `[本番]` |

---

## 🔄 日常的な開発フロー

### パターン1: 新機能開発

```bash
# ━━━━━━━━━━━━━━━━━━━━━━━━━━
# 1. 開発環境のみ起動
# ━━━━━━━━━━━━━━━━━━━━━━━━━━
sudo ./manage-env.sh prod stop
sudo ./manage-env.sh dev start

# ━━━━━━━━━━━━━━━━━━━━━━━━━━
# 2. ログを監視しながら開発
# ━━━━━━━━━━━━━━━━━━━━━━━━━━
# ターミナル1: ログ監視
./manage-env.sh dev logs

# ターミナル2: コード編集
nano frontend/app.js

# ━━━━━━━━━━━━━━━━━━━━━━━━━━
# 3. 変更を即座に反映
# ━━━━━━━━━━━━━━━━━━━━━━━━━━
sudo ./manage-env.sh dev restart

# ブラウザでテスト: https://192.168.0.187:5443

# ━━━━━━━━━━━━━━━━━━━━━━━━━━
# 4. テスト完了後にコミット
# ━━━━━━━━━━━━━━━━━━━━━━━━━━
git add .
git commit -m "feat: 新しいフィルター機能を追加"

# ━━━━━━━━━━━━━━━━━━━━━━━━━━
# 5. 本番にデプロイ
# ━━━━━━━━━━━━━━━━━━━━━━━━━━
sudo ./manage-env.sh prod restart

# ブラウザで確認: https://192.168.0.187:6443
```

### パターン2: バグ修正

```bash
# 1. 問題を開発環境で再現
sudo ./manage-env.sh dev start
# https://192.168.0.187:5443

# 2. ログでエラーを確認
tail -50 backend-dev.log

# 3. コード修正
nano backend/routes/incidents.js

# 4. 開発環境でテスト
sudo ./manage-env.sh dev restart

# 5. 修正を確認
# https://192.168.0.187:5443

# 6. コミット
git commit -m "fix: インシデント一覧のソートエラーを修正"

# 7. 本番にホットフィックス
sudo ./manage-env.sh prod restart
```

### パターン3: 並行開発

複数の機能を同時に開発する場合：

```bash
# 開発者A: 開発環境で新機能開発
# https://192.168.0.187:5443

# 開発者B: 本番環境でバグ調査（読み取り専用）
# https://192.168.0.187:6443

# 本番環境には影響を与えずに開発可能
```

---

## 🗄️ データベース管理ベストプラクティス

### 推奨構成

#### **環境別データベースの作成**

```bash
# 現在: 共通DB
backend/itsm_nexus.db

# 推奨: 環境別DB
backend/itsm_nexus_dev.db   # 開発環境専用
backend/itsm_nexus_prod.db  # 本番環境専用
```

#### **実装手順**

**1. 設定ファイルを更新**

`.env.development`:
```bash
DATABASE_PATH=./backend/itsm_nexus_dev.db
```

`.env.production`:
```bash
DATABASE_PATH=./backend/itsm_nexus_prod.db
```

**2. 開発DBを作成**

```bash
# 本番DBをコピー
cp backend/itsm_nexus.db backend/itsm_nexus_dev.db

# または新規作成
rm backend/itsm_nexus_dev.db
node -e "require('./backend/db').initDb()"
```

**3. サービスを再起動**

```bash
sudo ./manage-env.sh both restart
```

### 📝 データ同期戦略

#### **本番→開発へのデータ同期（週次推奨）**

```bash
#!/bin/bash
# sync-prod-to-dev.sh

echo "🔄 本番データを開発環境に同期中..."

# 1. 本番DBをバックアップ
BACKUP="backups/itsm_nexus_prod_$(date +%Y%m%d_%H%M%S).db"
cp backend/itsm_nexus_prod.db "$BACKUP"
echo "✅ 本番DBをバックアップ: $BACKUP"

# 2. 開発DBをバックアップ
DEV_BACKUP="backups/itsm_nexus_dev_$(date +%Y%m%d_%H%M%S).db"
cp backend/itsm_nexus_dev.db "$DEV_BACKUP"
echo "✅ 開発DBをバックアップ: $DEV_BACKUP"

# 3. 本番DBを開発DBにコピー
cp backend/itsm_nexus_prod.db backend/itsm_nexus_dev.db
echo "✅ 本番データを開発環境にコピー"

# 4. 個人情報をマスク（オプション）
sqlite3 backend/itsm_nexus_dev.db <<EOF
UPDATE users SET email = 'dev_' || id || '@example.com' WHERE role != 'admin';
UPDATE users SET full_name = 'Dev User ' || id WHERE role != 'admin';
EOF
echo "✅ 個人情報をマスク"

# 5. 開発環境を再起動
sudo ./manage-env.sh dev restart
echo "✅ 開発環境を再起動"

echo "🎉 同期完了！"
```

---

## 🧪 テスト戦略

### テストの種類と環境

| テスト種類 | 実行環境 | タイミング |
|-----------|---------|----------|
| **ユニットテスト** | ローカル | コミット前 |
| **統合テスト** | 開発環境 | プルリクエスト時 |
| **E2Eテスト** | 開発環境 | デプロイ前 |
| **負荷テスト** | 開発環境 | リリース前 |
| **スモークテスト** | 本番環境 | デプロイ直後 |

### 🎯 開発環境でのテスト例

```bash
# ユニットテスト
npm test

# 統合テスト
npm run test:integration

# E2Eテスト（開発環境で実行）
BASE_URL=https://192.168.0.187:5443 npm run test:e2e

# ログで結果確認
./manage-env.sh dev logs
```

---

## 🔐 セキュリティベストプラクティス

### 開発環境

✅ **許可される行為**
- デバッグログの有効化
- サンプルデータの使用
- エラーの詳細表示
- CORS設定を緩くする

❌ **禁止事項**
- 本番の機密データを使用しない
- 本番のAPIキーを使用しない
- セキュリティ機能を無効化しない

### 本番環境

✅ **必須設定**
- 強力なJWT_SECRET
- エラーの詳細を隠す
- レート制限を厳しく
- HSTSを有効化
- 定期的なセキュリティパッチ

❌ **禁止事項**
- デバッグモードで起動しない
- 不要なAPIエンドポイントを公開しない
- テストユーザーを放置しない

---

## 📦 リリース管理

### バージョニング戦略

セマンティックバージョニングを使用：

```
v2.1.0
│ │ │
│ │ └─ パッチバージョン（バグ修正）
│ └─── マイナーバージョン（新機能）
└───── メジャーバージョン（破壊的変更）
```

### CHANGELOGの管理

```markdown
## [2.1.1] - 2026-01-16

### Added
- インシデント管理にフィルター機能を追加

### Fixed
- SLA計算のバグを修正

### Changed
- ログイン画面のデザインを改善

### Security
- JWT検証ロジックを強化
```

---

## 🔄 継続的改善

### 週次レビュー

- [ ] 開発環境のログレビュー
- [ ] 本番環境のパフォーマンス確認
- [ ] バックアップの動作確認
- [ ] ディスク容量の確認

### 月次タスク

- [ ] SSL証明書の有効期限確認
- [ ] 依存パッケージの更新確認
- [ ] セキュリティパッチの適用
- [ ] ドキュメントの更新

---

## 🛠️ 便利なスクリプト集

### 環境切り替えスクリプト

```bash
#!/bin/bash
# switch-env.sh

case "$1" in
    dev)
        sudo ./manage-env.sh prod stop
        sudo ./manage-env.sh dev start
        echo "✅ 開発環境に切り替え: https://192.168.0.187:5443"
        ;;
    prod)
        sudo ./manage-env.sh dev stop
        sudo ./manage-env.sh prod start
        echo "✅ 本番環境に切り替え: https://192.168.0.187:6443"
        ;;
    both)
        sudo ./manage-env.sh both start
        echo "✅ 両方の環境を起動"
        echo "  開発: https://192.168.0.187:5443"
        echo "  本番: https://192.168.0.187:6443"
        ;;
    *)
        echo "使用方法: $0 {dev|prod|both}"
        exit 1
        ;;
esac
```

### ヘルスチェックスクリプト

```bash
#!/bin/bash
# health-check.sh

check_env() {
    local env=$1
    local url=$2

    echo "🔍 ${env}環境をチェック中..."

    # ヘルスエンドポイントを確認
    response=$(curl -sk "${url}/health" | jq -r '.status' 2>/dev/null)

    if [ "$response" == "OK" ]; then
        echo "✅ ${env}環境は正常"
    else
        echo "❌ ${env}環境に問題あり"
        return 1
    fi
}

check_env "開発" "https://192.168.0.187:5443"
check_env "本番" "https://192.168.0.187:6443"
```

---

## 📈 パフォーマンス監視

### メトリクス収集

開発環境と本番環境でパフォーマンスを比較：

```bash
# Prometheusメトリクスエンドポイント
# 開発環境
curl -s https://192.168.0.187:5443/metrics

# 本番環境
curl -s https://192.168.0.187:6443/metrics
```

### レスポンスタイム測定

```bash
# API応答時間を測定
time curl -k -H "Authorization: Bearer <token>" \
    https://192.168.0.187:5443/api/v1/incidents
```

---

## 🎓 チーム開発のガイドライン

### コミュニケーション

- デプロイ前にチームに通知
- 重大な変更はレビューを必須化
- 本番環境へのアクセスは記録

### ドキュメント規則

- 新機能はREADMEとCHANGELOGを更新
- API変更はopenapi.yamlを更新
- DBスキーマ変更はDATABASE_DESIGN.mdを更新

---

## ⚡ クイックリファレンス

### よく使うコマンド

```bash
# ━━━━━ 開発作業 ━━━━━
sudo ./manage-env.sh dev start      # 開発環境起動
sudo ./manage-env.sh dev restart    # 変更を反映
./manage-env.sh dev logs            # ログ監視
tail -f backend-dev.log             # ログファイル確認

# ━━━━━ 本番デプロイ ━━━━━
cp backend/itsm_nexus.db backups/  # DBバックアップ
sudo ./manage-env.sh prod restart   # 本番デプロイ
./manage-env.sh prod logs           # ログ監視
tail -f backend-prod.log            # ログファイル確認

# ━━━━━ 状態確認 ━━━━━
./manage-env.sh dev status          # 開発環境の状態
./manage-env.sh prod status         # 本番環境の状態
ss -tlnp | grep -E ":(5443|6443)"  # ポート確認

# ━━━━━ 自動起動 ━━━━━
sudo ./manage-env.sh both enable    # 両方自動起動ON
sudo ./manage-env.sh dev enable     # 開発のみ自動起動
sudo ./manage-env.sh prod enable    # 本番のみ自動起動
```

---

## 🎯 環境URL一覧

### アクセスURL

```
開発環境: https://192.168.0.187:5443
本番環境: https://192.168.0.187:6443
```

### APIドキュメント

```
開発環境: https://192.168.0.187:5443/api-docs
本番環境: https://192.168.0.187:6443/api-docs
```

### ヘルスチェック

```
開発環境: https://192.168.0.187:5443/health
本番環境: https://192.168.0.187:6443/health
```

---

## 🔍 環境識別方法

### ブラウザでの識別

| 識別方法 | 開発環境 | 本番環境 |
|---------|---------|---------|
| **URL** | `:5443` | `:6443` |
| **バッジ** | 🔧 緑色 | 🔒 赤色 |
| **タブタイトル** | `[開発]` | `[本番]` |
| **ブックマーク** | `[開発] ITSM-Sec Nexus` | `[本番] ITSM-Sec Nexus` |

### ログでの識別

```bash
# 開発環境のログ
[itsm-sec-nexus-dev] ...

# 本番環境のログ
[itsm-sec-nexus-prod] ...
```

---

## 📚 参考ドキュメント

- **開発ワークフロー詳細**: `DEVELOPMENT_WORKFLOW.md`
- **デプロイチェックリスト**: `DEPLOYMENT_CHECKLIST.md`
- **環境セットアップ**: `ENVIRONMENT_SETUP.md`
- **Systemdサービス管理**: `SYSTEMD_SERVICE.md`
- **データベース設計**: `DATABASE_DESIGN.md`

---

**最終更新**: 2026-01-16
**バージョン**: v2.1.0
